# Hash set duplicate pattern

Rattanak demonstrated the core idea for detecting duplicates: keep a set of previously seen values, check the current value against that memory, and return early when a repeat appears. Future lessons can build on the data-structure choice and control flow, while still reinforcing Python syntax for membership checks, colons, and indentation.
